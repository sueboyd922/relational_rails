<%= link_to "All Players", "/players" %> <%= link_to "All Teams", "/teams" %>
<h1>Fantasy Football Teams</h1>

<% @teams.each do |team| %>
   <div class="team-<%=team.id%>">
  <p><%= team.name %> <strong>created at</strong> <%= team.readable_date %> <%= link_to "Update", "/teams/#{team.id}/edit" %></p>
  </div>
  <% end %>

<%= link_to "New Team", "/teams/new" %>
